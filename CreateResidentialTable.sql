CREATE TABLE SDUNN_ROCHA_RESIDENTIAL (
    ListingNum      INT             NOT NULL,
    Pool            INT,
    NumBedRm        INT,
    NumBath         INT,
    NumLivingRm     INT,
    NumFloors       INT,
    PRIMARY KEY(ListingNum)
);

