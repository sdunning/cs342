CREATE TABLE SDUNN_SROCHA_COMMERCIAL (
    ListingNum      INT             NOT NULL,
    ParkingLot      INT             NOT NULL,
    PLSize          INT,
    NumRestRoom     INT,
    NumFloors       INT,
    PRIMARY KEY(ListingNum)
);

